biocypher:
  offline: true
  debug: false
  cache_directory: .cache

  head_ontology:
    url: data/biolink-model.owl.ttl
    root_node: entity

neo4j:
  delimiter: '\t'
  array_delimiter: "|"
  skip_duplicate_nodes: true
  skip_bad_relationships: true
  node_labels_order: Ascending
  edge_labels_order: Leaves
  # import_call_bin_prefix: /opt/homebrew/bin/   ← убрать